# Dataclass Generation Boundaries

This page is the second core of Module 06. It explains what `@dataclass` generates, which
surfaces it leaves untouched, and why generation is not the same thing as validation.
