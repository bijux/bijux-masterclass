# Visible Names and Stored State

This page is the first core of Module 02. It explains how `dir`, `vars`, `__dict__`, and
`__slots__` answer different questions so name discovery does not get confused with
physical object state.
