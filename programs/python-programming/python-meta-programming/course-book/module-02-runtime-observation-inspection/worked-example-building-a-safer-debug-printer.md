# Worked Example: Building a Safer Debug Printer

This worked example pressures the Module 02 boundaries in one realistic tool. It uses a
debug printer to show how observation can quietly become execution unless the tool makes
its lookup policy explicit.
