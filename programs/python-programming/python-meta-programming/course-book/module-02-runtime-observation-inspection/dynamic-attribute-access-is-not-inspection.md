# Dynamic Attribute Access Is Not Inspection

This page is the second core of Module 02. It explains why `getattr`, `setattr`,
`delattr`, and `hasattr` participate in the attribute protocol and can execute code.
