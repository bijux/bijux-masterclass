# Worked Example: Building a Partial `@validated` Decorator

This worked example pressures the Module 05 boundaries in one realistic wrapper. It uses a
partial runtime validator to show how annotation-aware policy can stay useful without
pretending to be a full type system.
