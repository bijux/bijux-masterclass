# Functions as Runtime Objects

This page is the first core of Module 01. It explains what a Python-defined function
really is at runtime, which metadata it carries, and where supported introspection ends.

Use it when a callable still feels like syntax instead of an object with identity,
environment, and behavior.
