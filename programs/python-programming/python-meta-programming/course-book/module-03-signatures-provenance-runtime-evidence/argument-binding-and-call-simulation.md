# Argument Binding and Call Simulation

This page is the second core of Module 03. It explains why `Signature.bind()` and
`BoundArguments` are the right tools when wrappers, validators, and adapters need to
simulate Python's call rules honestly.
