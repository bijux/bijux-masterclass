# Worked Example: Building a Deterministic Plugin Registry with `PluginMeta`

This worked example will combine definition-time enforcement and registration inside one
deliberately bounded metaclass case study.
