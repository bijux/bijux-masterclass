# Manual Class Creation with `type(...)`

This page will isolate the raw class-construction primitive so metaclasses can be learned
as an extension of class creation rather than as mysterious separate magic.
