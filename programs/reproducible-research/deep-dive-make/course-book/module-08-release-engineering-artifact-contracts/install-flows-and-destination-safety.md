# Install Flows and Destination Safety

This lesson will explain why `install` is a publication act with side effects, not a minor
appendix to `dist`. The final page will focus on staged installs, destination contracts,
idempotence, and safe overwrite behavior.
