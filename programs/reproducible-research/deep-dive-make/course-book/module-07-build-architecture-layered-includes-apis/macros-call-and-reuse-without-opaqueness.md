# Macros, call, and Reuse Without Opaqueness

This lesson will show how to reuse rule shapes with macros and `call` while keeping the
expanded graph auditable. The final page will focus on bounded reuse, readable arguments,
and the difference between enforcing invariants and hiding behavior.
