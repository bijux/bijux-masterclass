# CLI Semantics and Debug Control

This lesson teaches how to use Make's command-line interface as evidence. The full lesson
is developed in later commits in this branch history; the stable goal is already set:
learn which flags expose truth, which flags simulate conditions, and which flags can mask
real bugs when used carelessly.
