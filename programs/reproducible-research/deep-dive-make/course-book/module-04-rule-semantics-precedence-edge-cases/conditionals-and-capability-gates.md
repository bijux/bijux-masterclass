# Conditionals and Capability Gates

This lesson turns conditionals into architecture instead of scattered folklore. The final
version will show how to compute a capability once, name it clearly, and reuse it without
hiding platform or tool assumptions all over the build.
